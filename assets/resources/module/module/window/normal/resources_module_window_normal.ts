import { _decorator } from "cc";
import mk from "mk";
const { ccclass, property } = _decorator;

@ccclass("resources_module_window_normal")
export class resources_module_window_normal extends mk.view_base {
	// 初始化视图
	// create(): void {}
	// 有数据初始化
	// init(init_?: typeof this.init_data): void {}
	// 无数据初始化
	// open(): void {}
	// 模块关闭
	// close(): void {}
}
